; Central Telefonica - PJSIP
[global]
type=global
user_agent=Central-Telefonica
endpoint_identifier_order=ip,username,anonymous

; Carrier transport documented by VONO examples: SIP/5060.
[transport-udp]
type=transport
protocol=udp
bind=0.0.0.0:5060
allow_reload=yes

; Secure transport retained for providers/endpoints that support TLS.
[transport-tls]
type=transport
protocol=tls
bind=0.0.0.0:5061
cert_file=/etc/asterisk/tls/fullchain.pem
priv_key_file=/etc/asterisk/tls/privkey.pem
method=tlsv1_2
verify_client=no
verify_server=yes
allow_reload=yes

; WebRTC uses WSS + DTLS-SRTP.
[transport-wss]
type=transport
protocol=wss
bind=0.0.0.0

[vono-auth]
type=auth
auth_type=userpass
username=${VONO_AUTH_USERNAME}
password=${VONO_PASSWORD}

[vono-aor]
type=aor
contact=sip:${VONO_HOST}:${VONO_PORT}
qualify_frequency=30

[vono]
type=endpoint
transport=transport-udp
context=from-vono
disallow=all
allow=${VONO_CODECS}
aors=vono-aor
outbound_auth=vono-auth
from_user=${VONO_FROM_USER}
from_domain=${VONO_FROM_DOMAIN}
direct_media=no
rtp_symmetric=yes
force_rport=yes
rewrite_contact=yes
dtmf_mode=${VONO_DTMF_MODE}
send_pai=yes
trust_id_inbound=yes
trust_id_outbound=yes
media_encryption=${VONO_MEDIA_ENCRYPTION}

[vono-identify]
type=identify
endpoint=vono
match=${VONO_HOST}

; VONO's published examples use registration:
; usuario:senha@dominio:5060/usuario
[vono-registration]
type=registration
transport=transport-udp
outbound_auth=vono-auth
server_uri=sip:${VONO_HOST}:${VONO_PORT}
client_uri=sip:${VONO_USERNAME}@${VONO_HOST}
contact_user=${VONO_USERNAME}
retry_interval=30
forbidden_retry_interval=300
expiration=300
line=yes
endpoint=vono

[webrtc-template](!)
type=endpoint
transport=transport-wss
context=from-internal
disallow=all
allow=opus,ulaw,alaw,g722
webrtc=yes
dtls_auto_generate_cert=no
dtls_cert_file=/etc/asterisk/tls/fullchain.pem
dtls_private_key=/etc/asterisk/tls/privkey.pem
dtls_verify=fingerprint
dtls_setup=actpass
media_encryption=dtls
ice_support=yes
rtcp_mux=yes
use_avpf=yes
direct_media=no
dtmf_mode=rfc4733

[webrtc-aor-template](!)
type=aor
max_contacts=5
remove_existing=yes
qualify_frequency=30
